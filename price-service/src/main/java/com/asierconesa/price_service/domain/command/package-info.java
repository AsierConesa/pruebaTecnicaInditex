/**
 * Commands que contienen los datos enviados desde la capa
 * de entrada (REST, eventos, etc.) y se utilizan para
 * invocar la lógica de aplicación.
 */
package com.asierconesa.price_service.domain.command;
